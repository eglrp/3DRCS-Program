于2014.03.27日进行如下修改：
1、2014.03.26日开发，PSO算法设置为：粒子数目20；最大迭代次数5；控制误差0.01。
对所有三角面片进折叠点PSO计算，返回折叠点到FOT的最大距离value，经实验效率低下。

2、2014.03.27日开发，PSO算法设置参数不变，若三角面片的正则性<0.3，返回value为0，
进行优先折叠；否则返回value为最大距离。没有改变效率，此举的目的在于消灭狭长三角形。

3、2014.03.27日开发，此次目的在于提高算法的效率，具体开发过程如下：
  ——计算所有面片的正则性；
  ——若正则性小于0.3，扩大取值范围，进行PSO算法求折叠点，返回0；
      否则，运用3DRCS中以有的算法进行求解折叠点，返回value；
  ——增加三角形质量统计模块。

4、2014.03.27日，下一步打算，将PSO算法运用于网格模型平滑，光顺中去，以彻底消灭狭长三角形为目的。
